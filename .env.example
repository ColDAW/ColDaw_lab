# Environment Configuration for ColDaw

# Database Configuration
# Choose one: 'lowdb' (file-based), 'mongodb', or 'postgresql'
DATABASE_TYPE=lowdb

# MongoDB Configuration (if using MongoDB)
MONGODB_URI=mongodb://localhost:27017/coldaw

# PostgreSQL Configuration (if using PostgreSQL)
PGHOST=localhost
PGPORT=5432
PGDATABASE=coldaw
PGUSER=postgres
PGPASSWORD=your_password

# Railway automatically provides DATABASE_URL for their database services
# You can use either the specific variables above or DATABASE_URL
# DATABASE_URL=postgresql://user:password@host:port/database
# DATABASE_URL=mongodb://user:password@host:port/database

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
CLIENT_URL=http://localhost:5173

# JWT Configuration
JWT_SECRET=your-secret-key-here-make-it-long-and-random

# File Upload Configuration
MAX_FILE_SIZE=50mb
UPLOAD_DIR=uploads
PROJECTS_DIR=projects

# Railway Environment Variables (automatically set by Railway)
# RAILWAY_STATIC_URL - URL for static files
# RAILWAY_GIT_COMMIT_SHA - Git commit hash
# RAILWAY_GIT_BRANCH - Git branch name