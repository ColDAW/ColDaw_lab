# Environment Configuration for ColDaw - PostgreSQL Only

# Server Configuration
PORT=3001
NODE_ENV=development

# PostgreSQL Database Configuration (Required)
# Railway automatically provides DATABASE_URL when you add PostgreSQL service
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/coldaw

# Or use individual PostgreSQL connection parameters
PGHOST=localhost
PGPORT=5432
PGDATABASE=coldaw
PGUSER=postgres
PGPASSWORD=postgres

# CORS Configuration
CLIENT_URL=http://localhost:5173

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Redis Configuration (Railway will provide these)
REDIS_URL=redis://localhost:6379
# Alternative variable names that Railway might use:
# REDISCLOUD_URL=redis://user:password@host:port

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# JWT Configuration (Required)
JWT_SECRET=your-secret-key-here-make-it-long-and-random

# File Upload Configuration
MAX_FILE_SIZE=50mb
UPLOAD_DIR=uploads
PROJECTS_DIR=projects

# Railway Environment Variables (automatically set by Railway)
# RAILWAY_STATIC_URL - URL for static files
# RAILWAY_GIT_COMMIT_SHA - Git commit hash
# RAILWAY_GIT_BRANCH - Git branch name