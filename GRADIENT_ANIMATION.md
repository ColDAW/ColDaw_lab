# Apple Intelligence 风格渐变动画

## 功能特点

这是一个仿 Apple Intelligence 的渐变动画效果组件,具有以下特点:

### 🎨 渐变效果
- ✅ **从底部向上延伸**:渐变从卡片底部开始,逐渐向上扩展
- ✅ **水平不均匀分布**: 使用多层渐变,每层宽度和位置不同
- ✅ **动态效果**: 每层有独立的波动动画,营造流动感

### ⚡ 动画特性
- **多层叠加**: 5个渐变层以不同的延迟时间展开
- **波浪效果**: 4种不同的水平波动动画
- **辉光脉冲**: 模糊度、亮度和饱和度的动态变化
- **平滑过渡**: 使用 cubic-bezier 缓动函数

### 🎯 交互设计
- 点击按钮触发/关闭渐变效果
- 页面加载时文本元素淡入动画
- 按钮 3D 按压效果

## 使用方法

### 1. 访问演示页面
启动开发服务器后,访问:
```
http://localhost:5173/gradient-demo
```

### 2. 在其他页面中使用
```tsx
import GradientAnimation from '../components/GradientAnimation';

function YourPage() {
  return (
    <div>
      <GradientAnimation />
    </div>
  );
}
```

## 技术实现

### 渐变层配置
```tsx
<GradientLayer
  $isActive={isActive}      // 控制动画开关
  $delay={200}              // 延迟时间(ms)
  $width="90%"              // 层宽度
  $left="5%"                // 水平位置
  $animationType={2}        // 动画类型 (1-4)
/>
```

### 动画类型
- **Type 1**: 左右不对称波动,有垂直缩放
- **Type 2**: 先右后左,较慢的起伏
- **Type 3**: 不规则移动,多次变换
- **Type 4**: 简单的左右摆动

### 渐变配色
```css
linear-gradient(90deg,
  #89aaf8 20%,  /* 蓝色 */
  #b770fc 40%,  /* 紫色 */
  #d24dc3 60%,  /* 粉紫 */
  #e85560 80%,  /* 橙红 */
  #ee7b6b 100%  /* 橙色 */
)
```

## 自定义调整

### 修改颜色
在 `GradientSpan` 组件中修改 `background-image`:
```tsx
background-image: linear-gradient(90deg,
  #your-color-1 20%,
  #your-color-2 40%,
  // ...
);
```

### 调整模糊度
在 `GradientSpan` 中修改 `filter`:
```tsx
filter: blur(40px); // 增大数值=更模糊
```

### 调整动画速度
在 `GradientLayer` 中修改动画持续时间:
```tsx
animation: ... 5s ease-in-out infinite; // 修改 5s
```

### 添加更多层
在 `GradientContainer` 中复制添加更多 `GradientLayer`:
```tsx
<GradientLayer
  $isActive={isActive}
  $delay={500}
  $width="80%"
  $left="12%"
  $animationType={3}
/>
```

## 性能优化

- ✅ 使用 CSS transform 而非 position 属性
- ✅ 动画基于 GPU 加速的属性
- ✅ 条件渲染动画(仅在激活时运行)
- ✅ 使用 will-change 提示浏览器优化

## 浏览器兼容性

- Chrome/Edge 88+
- Firefox 85+
- Safari 14+
- 移动端浏览器完全支持

## 文件结构
```
client/src/
├── components/
│   └── GradientAnimation.tsx  # 主组件
└── pages/
    └── GradientDemoPage.tsx   # 演示页面
```

## 技术栈
- React 18
- TypeScript
- styled-components
- CSS Animations & Keyframes

---

**提示**: 可以通过调整渐变层的数量、位置、延迟时间和动画类型来创造不同的视觉效果!
