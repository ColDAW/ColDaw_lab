# Zoho Mail API 配置
# 用 Zoho 邮件服务替换 Mailgun 用于发送验证邮件

# Zoho Mail API 密钥 (OAuth Token)
# 从 Zoho Developer Console 获取
ZOHO_API_KEY=your_zoho_oauth_token_here

# Zoho 账户 ID (Account ID)
# 从 Zoho Mail 中获取你的账户 ID
ZOHO_ACCOUNT_ID=your_account_id_here

# 发送邮件的地址（必须是已验证的 Zoho 邮箱）
ZOHO_FROM_EMAIL=noreply@yourdomain.com

# （可选）其他 SMTP 备用配置
# 如果 Zoho API 不可用，系统会自动回退到 SMTP
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
SMTP_SECURE=true
SMTP_USER=your_zoho_email@yourdomain.com
SMTP_PASS=your_zoho_password

# 通用邮件配置
FROM_EMAIL=noreply@coldaw.app

# ============================================================
# 如何获取 Zoho 配置信息？
# ============================================================
#
# 1. 注册 Zoho 账户：
#    访问 https://www.zoho.com/mail/
#
# 2. 获取 Account ID：
#    登录 Zoho Mail
#    转到 设置 > 账户
#    复制 Account ID
#
# 3. 获取 OAuth Token：
#    访问 https://api-console.zoho.com/
#    创建新的 OAuth 应用
#    选择 Zoho Mail API 
#    设置重定向 URI (如: https://yourdomain.com/callback)
#    生成 OAuth Token
#
# 4. 验证发送邮箱：
#    在 Zoho Mail 中添加你要用作发送地址的邮箱
#    确保该邮箱已被验证
# ============================================================
